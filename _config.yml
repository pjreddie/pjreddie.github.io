collections:
    darknet:
        output: true
    courses:
        output: true
defaults:
    -
        scope:
            path: ""
            type: "darknet"
        values:
            layout: "darknet"
            order: 100
